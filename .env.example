# =============================================================================
# Curatr App - Environment Configuration
# =============================================================================
# Copy this file to .env and adjust values for your setup.
# For Docker, set these in docker-compose.yml environment section instead.

# =============================================================================
# DATABASE
# =============================================================================

# SQLite database location
# Local development: file:./prisma/dev.db
# Docker: file:/app/data/media-tracker.db
DATABASE_URL="file:./prisma/media-tracker.db"

# =============================================================================
# MEDIA LIBRARY PATHS (at least one required)
# =============================================================================

# Path to your TV shows directory
# Example: /media/TV Shows, /mnt/media/TV Shows
TV_SHOWS_PATH="/media/TV Shows"

# Path to your movies directory (for future use)
# Example: /media/Movies, /mnt/media/Movies
MOVIES_PATH="/media/Movies"

# =============================================================================
# TMDB INTEGRATION
# =============================================================================

# TMDB API Read Access Token for metadata, posters, and show matching
# Get yours at: https://www.themoviedb.org/settings/api
# Leave empty to disable TMDB features
TMDB_API_KEY=""

# =============================================================================
# FFPROBE INTEGRATION (Media Analysis)
# =============================================================================

# Path to ffprobe binary for extracting detailed media information
# Leave commented to disable media analysis feature
#
# Common paths:
#   Linux:                         /usr/bin/ffprobe
#   macOS (Homebrew, Apple Silicon): /opt/homebrew/bin/ffprobe
#   macOS (Homebrew, Intel):       /usr/local/bin/ffprobe
#   Docker (after apk add ffmpeg): /usr/bin/ffprobe
#
# FFPROBE_PATH="/usr/bin/ffprobe"

# FFprobe execution timeout in milliseconds (default: 30 seconds)
# Increase for very large files or slow storage
# FFPROBE_TIMEOUT=30000

# =============================================================================
# SCANNER SETTINGS
# =============================================================================

# Number of parallel file operations during scan (default: 4)
# Lower this if you experience I/O issues on slow storage
# SCAN_CONCURRENCY=4

# Number of files to process per database batch (default: 100)
# Lower this to reduce memory usage, higher for faster scans
# SCAN_BATCH_SIZE=100

# =============================================================================
# TASK SYSTEM
# =============================================================================

# How long to keep completed tasks in memory for review (default: 1 hour)
# Value in milliseconds (3600000 = 1 hour)
# TASK_RETENTION_MS=3600000

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Authentication mode: "none" (default) or "plex"
# - none: No login required, all features available (single-user / self-hosted)
# - plex: Users log in with their Plex account, role-based access control
AUTH_MODE="none"

# Plex Authentication (required when AUTH_MODE=plex)
# Your Plex server URL (e.g., http://192.168.1.100:32400)
# PLEX_URL=""

# Your Plex authentication token
# Find it: Plex Web → Settings → open any XML page → check the URL for "X-Plex-Token="
# PLEX_TOKEN=""

# =============================================================================
# PLEX INTEGRATION (Future Feature)
# =============================================================================

# Path to Plex SQLite database for metadata sync (read-only)
# Usually: /var/lib/plexmediaserver/Library/Application Support/Plex Media Server/Plug-in Support/Databases/
# PLEX_DB_PATH="/plex-db"
